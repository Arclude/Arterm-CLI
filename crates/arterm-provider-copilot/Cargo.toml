[package]
name = "arterm-provider-copilot"
version = "0.1.0"
edition = "2024"

[dependencies]
arterm-message-types = { path = "../arterm-message-types" }
arterm-schema-dialect = { path = "../arterm-schema-dialect" }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
