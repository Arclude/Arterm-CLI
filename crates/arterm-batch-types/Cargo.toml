[package]
name = "arterm-batch-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
arterm-message-types = { path = "../arterm-message-types" }
serde = { version = "1", features = ["derive"] }
