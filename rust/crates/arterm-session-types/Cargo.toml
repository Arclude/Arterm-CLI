[package]
name = "arterm-session-types"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
chrono = { version = "0.4", features = ["serde"] }
arterm-message-types = { path = "../arterm-message-types" }
memchr = "2"
serde = { version = "1", features = ["derive"] }
