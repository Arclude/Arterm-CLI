[package]
name = "arterm-tool-types"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Stable tool output/result data contracts and tool-name alias resolution."

# A contract crate: data and pure functions only. Config and provider layers
# normalize tool names through here without depending on the tool subsystem.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
